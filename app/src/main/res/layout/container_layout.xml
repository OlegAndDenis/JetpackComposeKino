<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/back">

    <androidx.appcompat.widget.Toolbar
        android:id="@+id/container_toolbar"
        android:layout_width="match_parent"
        android:background="?colorToolbar"
        app:titleTextColor="?colorText"
        android:layout_height="?actionBarSize" />

    <FrameLayout
        android:id="@+id/container_frame"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="?colorBackground"
        android:layout_above="@+id/container_bottom_navigation"
        android:layout_below="@+id/container_toolbar" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/container_bottom_navigation"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="?colorToolbar"
        app:itemIconTint="@drawable/bottom_selections"
        app:itemTextColor="@drawable/bottom_selections"
        app:labelVisibilityMode="labeled"
        app:menu="@menu/navigation" />
</RelativeLayout>